#!/bin/bash

if [ -x /opt/bin/dmenu ]; then
	DMENU='/opt/bin/dmenu'
elif [ -x /usr/bin/dmenu ]; then
	DMENU='/usr/bin/dmenu'
else
	exit 0
fi

$DMENU -fn 'Meslo LG M-14' -nb '#303030' -nf '#C6C6C6' -sb '#afd700' -sf '#005f00' $@
