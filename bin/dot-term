#!/bin/bash

ALACRITTY=$(which alacritty 2>/dev/null)
if [ -n "$ALACRITTY" ]; then
	alacritty "$@"
elif [ -x "/opt/bin/sakura" ]; then
	/opt/bin/sakura "$@"
else
	TERMINATOR=$(which terminator 2>/dev/null)

	if [ -n "$TERMINATOR" ]; then
		terminator "$@"
	else
		xterm -maximized "$@"
	fi
fi
