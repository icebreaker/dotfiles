#!/bin/sh

if [ $# -lt 1 ]; then
	echo "usage: `basename $0` PORT"
	exit -1
fi

FROM=$2
PROTO=$3

if [ -z "$FROM" ] || [ "$FROM" = "-" ]; then
	FROM="192.168.1.0/24"
fi

if [ -z "$PROTO" ]; then
	PROTO="tcp"
fi

sudo ufw allow from $FROM to any port $1 proto $PROTO && sudo ufw status numbered
