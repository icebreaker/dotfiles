#!/bin/sh

if [ $# -lt 1 ]; then
	echo "usage: `basename $0` PORT"
	exit -1
fi

FROM=$2

if [ -z "$FROM" ]; then
	FROM="192.168.1.0/24"
fi

sudo ufw allow from $FROM to any port $1 proto tcp && sudo ufw status numbered
