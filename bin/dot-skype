#!/bin/bash

if [ -z "$BROWSER" ]; then
	BROWSER=google-chrome-launcher

	if [ -z "$DOT_BIN" ]; then
		CWD="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
		BROWSER="$CWD/$BROWSER"
	fi

	export BROWSER
fi

SKYPE_BIN=skypeforlinux

$SKYPE_BIN "@$"
